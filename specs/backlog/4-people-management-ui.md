---
issue: 4
label: needs-triage
---

# People management UI

Build the "add a person" form and "list all people" view against MSW mocks. Scoped as its own issue because it's a self-contained screen with no dependency on expense logic, and the person list it produces is what the expense form's participant/payer picker will need.

Dependencies: #1, #2, #3.
