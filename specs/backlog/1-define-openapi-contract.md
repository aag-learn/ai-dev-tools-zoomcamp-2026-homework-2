---
issue: 1
label: needs-triage
---

# Define OpenAPI contract for v1 endpoints

Write `openapi/openapi.yaml` covering every endpoint the v1 scope implies: add/list people, add/edit/delete/list expenses, view balances. This is its own issue because the build order requires it to be fully settled before any frontend or backend work starts — it's the single gate the rest of the backlog depends on.

Dependencies: none.
