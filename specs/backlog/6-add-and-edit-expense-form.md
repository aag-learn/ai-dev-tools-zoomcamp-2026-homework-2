---
issue: 6
label: needs-triage
---

# Add and edit expense form

Build a shared form component for adding and editing an expense (description, amount, payer, date, participants — defaulting to everyone currently in the group), against MSW mocks. Combined into one issue because add and edit share the same form and validation; kept separate from the list view because it's a materially different, more complex UI surface (person picker, equal-split logic, defaulting behavior).

Dependencies: #1, #2, #3, #4 (needs the person list/picker).

Note: filed on GitHub as "Add and edit expense form" (a slash in the originally drafted title "Add/Edit expense form" was rejected at filing time) — no change in scope.
