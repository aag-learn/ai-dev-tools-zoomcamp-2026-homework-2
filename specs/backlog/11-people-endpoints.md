---
issue: 11
label: needs-triage
---

# People endpoints

Implement `POST /people` and `GET /people` per the contract, with pytest coverage. Scoped as its own issue because it's the simpler CRUD surface and a prerequisite for expenses, which reference people.

Dependencies: #1, #8, #9.
